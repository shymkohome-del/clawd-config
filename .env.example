# Example environment configuration for Crypto Market
# Copy this file to .env (do NOT commit real secrets) and fill in values.
# Production/staging values must be managed via CI/CD secrets, not files.

# OAuth (Google)
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=

# OAuth (Apple) - placeholders OK for early dev
OAUTH_APPLE_TEAM_ID=dev-placeholder
OAUTH_APPLE_KEY_ID=dev-placeholder

# IPFS (use Infura or local node)
# Example Infura node URL: https://ipfs.infura.io:5001
# Example public gateway: https://ipfs.io/ipfs/
IPFS_NODE_URL=https://ipfs.infura.io:5001
IPFS_GATEWAY_URL=https://ipfs.io/ipfs/

# ICP Canister IDs
CANISTER_ID_MARKETPLACE=
CANISTER_ID_USER_MANAGEMENT=
CANISTER_ID_ATOMIC_SWAP=
CANISTER_ID_PRICE_ORACLE=

# Optional API keys (leave empty if not used)
# CHAINLINK_API_KEY=
# COINGECKO_API_KEY=
# KYC_PROVIDER_API_KEY=

# Notes:
# - Local development can pass values via --dart-define (preferred) or load from this file using a local-only .env loader.
# - The Flutter app reads values primarily from --dart-define.

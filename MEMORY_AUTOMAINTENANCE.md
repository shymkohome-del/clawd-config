# 🤖 Автоматична підтримка векторної пам'яті Clawdbot

## 📋 Що автоматизовано

### 1. Регулярна реіндексація (Cron)
- **Кожні 15 хвилин**: Швидка перевірка та фікс
- **Щодня о 3:00**: Повна реіндексація

### 2. Моніторинг цілісності
- Перевірка кількості векторів vs chunks
- Автоматичне виправлення прав на базу
- Відстеження помилок readonly

### 3. Pre-session hook
- Виправлення прав перед стартом сесії
- Фонова реіндексація

## 🛠️ Команди

```bash
# Швидка перевірка
~/clawd/scripts/ckmem.sh

# Ручний фікс
~/clawd/scripts/clawdbot-memory-maintenance.sh

# Перевірка логів
 tail -f /tmp/clawdbot-memory-maintenance.log
```

## 📁 Файли

| Файл | Призначення |
|------|-------------|
| `~/clawd/scripts/clawdbot-memory-maintenance.sh` | Основний скрипт обслуговування |
| `~/clawd/scripts/ckmem.sh` | Швидка перевірка статусу |
| `~/clawd/scripts/clawdbot-alert.sh` | Сповіщення про проблеми |
| `~/.clawdbot/hooks/pre-session-check` | Hook перед сесією |
| `/tmp/clawdbot-memory-maintenance.log` | Логи обслуговування |
| `/tmp/clawdbot-alerts.log` | Логи сповіщень |

## ⚙️ Налаштування

Додай в `~/.zshrc`:

```bash
alias ckm='clawdbot memory status'
alias cki='clawdbot memory index'
alias cks='clawdbot memory search'
alias cklogs='tail -f /tmp/clawdbot-memory-maintenance.log'
alias ckfix='~/clawd/scripts/clawdbot-memory-maintenance.sh'
```

## 🔄 Рівні автоматизації

```
Рівень 1: Cron (кожні 15 хв) ──┐
                               ├──→ Автоматичне відновлення
Рівень 2: Pre-session hook ────┤
                               │
Рівень 3: Alert system ────────┘
```

## 📊 Моніторинг

Перевірити статус вручну:
```bash
# Загальний статус
clawdbot memory status

# Перевірка через скрипт
~/clawd/scripts/ckmem.sh

# Логи
 cat /tmp/clawdbot-memory-maintenance.log
```

## 🚨 Ручне втручання

Якщо автоматика не справляється:

```bash
# 1. Зупинити gateway
pkill -f "clawdbot gateway"

# 2. Виправити права
chmod 666 ~/.clawdbot/memory/main.sqlite

# 3. Перезапустити
nohup clawdbot gateway > /tmp/clawdbot-gateway.log 2>&1 &

# 4. Повна реіндексація
clawdbot memory index
```

---
Created: $(date '+%Y-%m-%d')

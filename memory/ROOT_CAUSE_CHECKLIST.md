# Root Cause Analysis Checklist
## Перед будь-яким виправленням використовувати!

## 🚫 HARDCODING PRE-CHECK (CRITICAL)

**Before ANY edit, verify:**
- [ ] I am NOT typing canister IDs manually
- [ ] I am NOT copy-pasting principals/keys
- [ ] I am NOT editing .env files directly with real values
- [ ] I will use scripts/tools to generate configs
- [ ] Values will be read from: `canister_ids.json`, identity files, etc.

**If answer to any is NO → STOP and find the right way!**

---

## 🔍 5 Whys (5 разів запитати "Чому?")

**Приклад:**
1. **Симптом:** Config shows "Out of Sync"
   - Чому? → .env має null замість ID
   
2. **Чому .env має null?**
   - Чому? → Sync script не оновив значення
   
3. **Чому sync script не оновив?**
   - Чому? → Він шукає ключ "ic" але в JSON ключ "staging"
   
4. **Чому він шукає неправильний ключ?**
   - Чому? → NETWORK=ic але canister_ids.json використовує flavor names
   
5. **Чому логіка не враховує це?**
   - Корінь: Скрипт не має маппінгу network → canister_key

✅ **Root Cause знайдено!**

---

## 📋 Обов'язкові питання перед фіксом:

- [ ] Я знайшов файл/скрипт який ГЕНЕРУЄ цю проблему?
- [ ] Я розумію логіку чому це відбувається?
- [ ] Мій фікс виправляє ПРИЧИНУ а не наслідок?
- [ ] Цей фікс запобігатиме повторенню проблеми?
- [ ] Я перевірив що немає інших місць з тією ж проблемою?

---

## 🚫 Червоні прапорці (симптоми лікування):

- Редагую .env файли вручну ← ЛІКУЮ СИМПТОМ
- Копіюю значення з одного файлу в інший ← ЛІКУЮ СИМПТОМ  
- Додаю if/else для обходу проблеми ← ЛІКУЮ СИМПТОМ
- Не розумію чому це спрацювало ← ЛІКУЮ СИМПТОМ

## ✅ Зелені прапорці (корінь проблеми):

- Виправив скрипт який генерує дані ← КОРІНЬ
- Змінив логіку яка призводить до помилки ← КОРІНЬ
- Розумію повний ланцюжок від причини до наслідку ← КОРІНЬ
- Проблема не повториться після мого фіксу ← КОРІНЬ

---

## 📝 Шаблон звіту про фікс:

```
**Симптом:** [Що бачив користувач]
**Корінь проблеми:** [Чому це відбувалось]
**Фікс:** [Що змінено]
**Профілактика:** [Чому це не повториться]
```

**Приклад:**
```
**Симптом:** Config показує "Out of Sync"
**Корінь:** run-sync-config.sh використовував NETWORK=ic замість CANISTER_KEY=staging
**Фікс:** Додав маппінг network → canister_key в скрипт
**Профілактика:** Тепер скрипт правильно знаходить canister IDs для staging
```

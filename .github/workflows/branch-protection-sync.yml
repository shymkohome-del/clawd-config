name: Branch Protection Sync (Disabled - Missing PAT)

on:
  workflow_dispatch:
  # Disabled due to missing BRANCH_PROTECTION_PAT secret
  # schedule:
  #   # Run daily at 6 AM UTC to ensure branch protection stays in sync
  #   - cron: '0 6 * * *'
  # push:
  #   branches:
  #     - develop
  #   paths:
  #     - '.github/workflows/**'

permissions:
  contents: read
  actions: read

jobs:
  disabled:
    name: Workflow Disabled - Missing Secret
    runs-on: ubuntu-latest
    steps:
      - name: Missing BRANCH_PROTECTION_PAT Secret
        run: |
          echo "❌ This workflow is disabled because BRANCH_PROTECTION_PAT secret is not configured"
          echo "ℹ️  To enable this workflow:"
          echo "   1. Create a Personal Access Token with 'repo' scope"
          echo "   2. Add it as a repository secret named 'BRANCH_PROTECTION_PAT'"
          echo "   3. Re-enable the workflow triggers in this file"
          echo "✅ Workflow completed (no-op)"
